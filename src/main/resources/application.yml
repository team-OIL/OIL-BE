server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/oil?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: oil_user
    password: 1592!Witdlggcxz
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true

  jackson:
    serialization:
      write-dates-as-timestamps: false

  data:
    redis:
      host: localhost
      port: 6379


# Swagger ?? (Springdoc)
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace

jwt:
  secret: "THIS_IS_SECRET_KEY_CHANGE_IT_TO_REAL_VALUE"
  access-token-expiration: 3600000     # 1시간 (ms)
  refresh-token-expiration: 1209600000 # 14일 (ms)
  header: Authorization
  prefix: "Bearer "


cloud:
  aws:
    credentials:
      access-key: YOUR_ACCESS_KEY
      secret-key: YOUR_SECRET_KEY
    s3:
      bucket: YOUR_BUCKET_NAME
    region:
      static: ap-northeast-2
